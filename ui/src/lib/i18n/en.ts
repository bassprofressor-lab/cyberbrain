/**
 * English source dictionary. Every user-visible string in the page lives here; `de.ts` is
 * declared as `Dict` so a missing or misshapen entry fails the build instead of showing an
 * English fragment in a German page.
 *
 * Not in here, on purpose: text the binary produces (recall caveats, doctor findings, audit
 * actions, error messages from the API). Those arrive over the wire in the server's own
 * words; translating them means translating the Rust side, not this file.
 */
export const en = {
  app: {
    mockTitle: "MOCK DATA",
    mockBody: "No backend. Every number on every screen is fabricated for layout; the compliance statement is not evidence of anything.",
    connected: (path: string) => `connected to same-origin ${path}`,
    themeTitle: "Theme: system → light → dark",
    themeSystem: (effective: string) => `system (${effective})`,
    themeDark: "dark",
    themeLight: "light",
    languageTitle: "Language: English → Deutsch",
    hubAlone: "This machine keeps everything to itself.",
    hubAloneWhy: "Nothing is delivered anywhere. Notes never leave a machine either way; what a company hub collects is the record of what happened, not what was written.",
    hubReports: "Reporting to",
    hubLastDelivery: (when: string) => `last delivered ${when}`,
    hubNothingYet: "nothing delivered yet",
    hubWhatLeaves: "What leaves: the audit trail — which machine did what kind of thing, when. Never the content of a note.",
  },
  nav: {
    status: "Status",
    search: "Search",
    notes: "Notes",
    graph: "Graph",
    usage: "Usage",
    compliance: "Compliance",
    console: "Command line",
    terminals: "Terminal",
    team: "In the team",
  },
  scopes: {
    global: "global",
    status: "status",
    search: "search",
    notes: "notes",
    note: "note",
    graph: "graph",
    usage: "usage",
    compliance: "compliance",
    console: "command line",
    terminals: "terminal",
    team: "in the team",
  },
  common: {
    refresh: "refresh",
    retry: "retry",
    cancel: "cancel",
    close: "close",
    open: "open",
    edit: "edit",
    none: "none",
    never: "never",
    yes: "yes",
    no: "no",
    on: "on",
    off: "off",
    all: "all",
    or: "or",
    loading: "loading",
    searching: "searching…",
    ring: "ring",
    intent: "intent",
    indefinite: "indefinite",
    notStated: "not stated",
    tokens: "tokens",
    days: (n: number) => `${n} days`,
    dayShort: (n: number) => `${n} d`,
  },
  units: {
    year: (n: number) => `${n} year${n === 1 ? "" : "s"}`,
    month: (n: number) => `${n} month${n === 1 ? "" : "s"}`,
    week: (n: number) => `${n} week${n === 1 ? "" : "s"}`,
    day: (n: number) => `${n} day${n === 1 ? "" : "s"}`,
  },
  rings: {
    /**
     * The same five tiers said without the vocabulary, for simple mode. A colleague who has
     * never read the spec cannot act on "invariant"; "always applies" is the same fact.
     */
    plain: ["Always applies", "How we work", "Knowledge", "From a session", "From outside"],
    label: ["Invariant", "Protocol", "Knowledge", "Session", "External"],
    help: [
      "Operator invariants. Hard rules. Override all higher rings; always injected.",
      "Operating protocol and handoff state. Override 2+; always injected.",
      "Curated project knowledge. Retrieved on demand.",
      "Session records and observations. Retrieved on demand, lower weight.",
      "Imported or unverified material. Retrieved last, marked unverified.",
    ],
  },
  keys: {
    goTo: (label: string) => `Go to ${label}`,
    search: "Search",
    toggleTheme: "Toggle theme",
    toggleLanguage: "Switch language",
    showShortcuts: "Show shortcuts",
    close: "Close",
    title: "Keyboard",
    dialogLabel: "Keyboard shortcuts",
    hint: { toggles: "toggles", closes: "closes" },
  },
  markdown: {
    image: "image",
    imageTitle: "images are not loaded; the UI makes no outbound requests",
    wikilinkIntent: (name: string) => `[[${name}]] has no note yet. Intent, not an error; doctor lists it as dangling.`,
  },
  pii: {
    state: { unscanned: "not scanned", none: "scanned, nothing found", reviewed: "reviewed", flagged: "flagged" },
  },
  citation: {
    copy: "Copy citation",
    copyAria: (citation: string) => `Copy citation ${citation}`,
    copied: (citation: string) => `copied ${citation}`,
    copiedN: (n: number) => `copied ${n} citations`,
    clipboardUnavailable: "clipboard unavailable",
  },
  error: {
    http: (status: string | number, exit: number) => `http ${status} · exit ${exit}`,
  },
  status: {
    title: "Status",
    loading: "reading status",
    keys: { refresh: "Refresh", doctor: "Run doctor" },
    btn: {
      doctor: "doctor",
      doctorBusy: "checking…",
      scan: "scan",
      scanBusy: "scanning…",
      full: "scan --full",
      fullBusy: "rebuilding…",
      fullTitle: "Rebuild blocks, vectors and links for every note",
    },
    scanToast: (r: { changed: number; added: number; removed: number; ms: number }) => `scan: ${r.changed} changed, ${r.added} added, ${r.removed} removed in ${r.ms} ms`,
    stat: {
      store: "store",
      storeSub: (notes: string, blocks: string, vectors: string) => `${notes} notes · ${blocks} blocks · ${vectors} vectors`,
      index: "index",
      indexFresh: "fresh",
      indexStale: (n: number) => `${n} stale`,
      indexSub: (last: string, full: string) => `last scan ${last} · full ${full}`,
      embedding: "embedding",
      embeddingNone: "no model loaded",
      embeddingLexical: "search is lexical only",
      embeddingNothing: "nothing to compare yet",
      embeddingMatches: "profile matches the index",
      embeddingMismatch: "PROFILE MISMATCH — semantic search off",
      backend: "last inference backend",
      backendAnswered: (when: string) => `answered ${when}`,
      backendNoCall: "no call yet",
      backendUnknown: "unknown",
    },
    caveats: "what this page cannot measure",
    mismatch: {
      lead: "Semantic search is disabled.",
      body: (profile: string) => `The configured embedding profile ${profile} does not match the vectors in the index. Recall runs lexical-only and says so. Run scan --full to reindex; comparing vectors across models would degrade results silently, which is why it is not allowed.`,
    },
    store: {
      title: "Store",
      notes: "notes/",
      notesValue: (size: string) => `${size} · Markdown only, authoritative`,
      db: "cyberbrain.db",
      dbValue: (size: string) => `${size} · index and vectors; rebuildable (audit.db is separate and is not)`,
      models: "models/",
      modelsValue: (size: string) => `${size} · content-addressed artefacts`,
      cap: "resident cap · rings 0+1",
      capValue: (used: string, total: string, pct: number) => `${used} / ${total} tokens · ${pct}%`,
      capNote: "Injected into every session. Exceeding the cap is a write-time error, never a read-time truncation.",
      blocksPerRing: "blocks per ring",
      ringCounts: (notes: string, blocks: string, tokens: string) => `${notes} n · ${blocks} b · ${tokens} t`,
    },
    index: {
      title: "Index",
      schema: (v: number) => `schema v${v}`,
      lastScan: "last scan",
      lastFull: "last full scan",
      stale: "stale notes",
      staleHint: "(mtime or hash differs from the index)",
      orphans: "orphan vectors",
      dangling: "dangling links",
      danglingHint: "(intent and unresolvable together; doctor tells them apart)",
      fts: "FTS5",
      ftsOk: "ok",
      ftsBroken: "broken",
      dryRun: "dry run",
      scanLine: (s: { scanned: number; changed: number; added: number; removed: number; ms: number; blocks: string; vectors: string }) =>
        `${s.scanned} listed, ${s.changed} changed, ${s.added} added, ${s.removed} removed in ${s.ms} ms · index now holds ${s.blocks} blocks, ${s.vectors} vectors`,
      scanDetail: (d: { unchanged: number; touched: number; revectorised: number; links: number }) =>
        `${d.unchanged} unchanged · ${d.touched} touched only · ${d.revectorised} revectorised · ${d.links} links written back`,
      scanSkipped: (n: number) => ` · ${n} skipped`,
      scanOversized: (n: number) => ` · ${n} oversized blocks`,
      scanNoEmbedder: (reason: string) => ` · no embedder (${reason})`,
      noReason: "no reason given",
    },
    doctor: {
      clean: "clean",
      errors: "found errors",
      warnings: "found warnings",
      summary: (findings: number, checks: number, when: string) => `${findings} findings · ${checks} checks ran · ${when}`,
      checks: (list: string) => `checks: ${list}`,
      danglingIntent: "dangling: intent",
      danglingIntentHint: "A valid note name that does not exist yet. Somebody will write it; not an error.",
      unresolvable: "unresolvable link",
      unresolvableHint: "A name that can never be a note (capitals, underscores, a path). A typo or another tool's naming; waiting will not fix it.",
      checkHint: (check: string) => `check: ${check}`,
    },
    embedding: {
      title: "Embedding profile",
      noModel: "no model loaded",
      nothingToCompare: "nothing to compare",
      nothingToCompareTitle: "no model loaded or no vectors stored",
      matches: "matches index",
      mismatch: "mismatch",
      warning: "No embedding model is loaded; the fields below describe the profile the index last recorded, not a live model. Semantic search is off and recall says so.",
      profileId: "profile id",
      model: "model",
      dimension: "dimension",
      backend: "backend",
      backendStatic: "token lookup, no transformer at query time",
      backendCandle: "candle transformer",
      hash: "artefact hash",
      hashNone: "none: no artefact and no profile recorded",
      verified: "verified",
      verifiedAt: (when: string) => `${when} on load · mismatch is a hard failure`,
      verifiedNoStamp: "hash checked on every load; no timestamp of that check is kept",
    },
    inference: {
      title: "Inference endpoint",
      notConfigured: "not configured",
      baseUrl: "base url",
      addressClass: "address class",
      allowed: " · allowed by allow_public_endpoint = true",
      refused: " · REFUSED: allow_public_endpoint is false",
      local: " · stays on this machine or your network",
      model: "model",
      reachable: "reachable",
      notProbed: "not probed for this request",
      probedBy: (when: string) => `(${when}, probed by the binary, never by this page)`,
      lastCall: "last call",
      noAuditRow: "no inference.call row in the audit log",
      whichBackend: "which backend answered last",
      evidence: (e: string) => `evidence: ${e}`,
      fingerprint: "identified from the response fingerprint; unknown means the server matched no known signature, which is not an error",
      none: "No inference endpoint configured. Every core function works without one; contradiction checks, ring proposals and session summaries are off, and recall says so in its caveats.",
    },
    footer: {
      ledgerMoved: "token ledger and model load moved to",
      profile: "policy profile",
      piiScan: "PII scan",
      auditRows: (n: string) => `${n} audit rows`,
      compliance: "compliance",
    },
  },
  search: {
    placeholder: "Recall… (hybrid: lexical + semantic, fused, ring-weighted)",
    aria: "Recall query",
    hitsAria: "Hits",
    nAria: "Number of hits",
    nTitle: "n — hits returned",
    nOption: (n: number) => `n = ${n}`,
    examples: ["postgres data directory", "hook budget", "why no hf-hub", "windows cmd.exe quoting", "contradiction lower ring"],
    emptyTitle: "Type to recall. Every hit carries a citation you can paste back to an agent.",
    stats: (mode: string, ms: number, kLex: number, kSem: number) => `${mode} · ${ms} ms · k_lex ${kLex} · k_sem ${kSem}`,
    keysTitle: "keys",
    ringsTitle: "rings",
    keyList: {
      focus: "focus query",
      move: "move selection",
      copy: "copy citation of selected hit",
      copyAll: "copy all citations, one per line",
      open: "open the note at that block",
      expand: "expand selected block",
      help: "all shortcuts",
    },
    scoring: "Score = RRF over lexical and semantic ranks × ring weight [1.15 1.10 1.00 0.92 0.80]. Lower ring wins a contradiction; both citations are shown.",
    conflicts: (n: number) => `${n} contradiction${n === 1 ? "" : "s"} reported — lower ring wins, both kept`,
    wins: "wins",
    loses: "loses",
    noHits: (q: string, ring: number | null) => `No block matched "${q}"${ring !== null ? ` in ring ${ring}` : ""}.`,
    tryAllRings: "Try all rings.",
    tryWords: "Recall is over blocks, not titles. Try the words that would appear in the paragraph.",
    srcUnknown: "src n/a",
    srcUnknownTitle: "The index does not report which candidate list (lexical, semantic) this block came from; see the caveats below.",
    scoreTitle: (score: number) => `fused, ring-weighted score ${score}`,
    keys: {
      focus: "Focus query",
      next: "Next hit",
      prev: "Previous hit",
      copy: "Copy citation of selected hit",
      copyAll: "Copy all citations",
      open: "Open note of selected hit",
      expand: "Expand / collapse selected hit",
      back: "Back to query",
    },
  },
  notes: {
    filterPlaceholder: "filter by name or tag",
    filterAria: "Filter notes",
    kindAria: "Kind",
    anyKind: "any kind",
    listAria: "Notes",
    noMatch: "no notes match",
    count: (n: number) => `${n} note${n === 1 ? "" : "s"}`,
    selectTitle: "Select a note.",
    selectHint: { move: "to move,", open: "to open,", edit: "to edit." },
    danglingTitle: (n: number) => `${n} dangling link${n === 1 ? "" : "s"} (intent)`,
    piiNotScanned: "not scanned",
    pii: (state: string) => `pii ${state}`,
    keys: { next: "Next note", prev: "Previous note", open: "Open note under cursor", filter: "Filter notes", edit: "Edit open note" },
  },
  note: {
    loading: "loading note",
    piiNotScanned: "pii not scanned",
    piiNotScannedTitle: "No write-time scan ever ran over this note. Writing it through the tool scans it.",
    write: "write to disk",
    writing: "writing…",
    forget: "forget…",
    forgetTitle: "Runs forget with --dry-run first and shows what would go",
    wrote: (path: string) => `wrote ${path}`,
    notShared: "not shared",
    field: { id: "id", path: "path", created: "created", updated: "updated", retention: "retention", bereich: "bereich", ring: "ring", tags: "tags" },
    bodyAria: "Note body",
    bodyNote: "Body only. Frontmatter fields are edited above the fold in a later version; id, created, updated, links and pii are server-owned. The write goes through the PII scan for profile eu/ch and is reindexed in the same request.",
    citations: (n: number) => `citations · ${n} blocks`,
    blockLabel: (idx: number) => `block ${idx}`,
    outbound: (n: number) => `outbound · ${n}`,
    inbound: (n: number) => `inbound · ${n}`,
    noInbound: "nothing links here",
    intentTitle: "No note with this name yet. Intent, not an error.",
    keys: { save: "Save", cancelEdit: "Cancel edit", copyFirst: "Copy first citation" },
    hold: {
      aria: "Write held: personal data found",
      badge: "write held",
      title: (note: string) => `Possible personal data in ${note}`,
      body: "Profile eu holds the write until you decide (SPEC §12.4). Detection is heuristic: a seatbelt, not a guarantee. Nothing has been written yet.",
      colKind: "kind",
      colExcerpt: "excerpt (masked)",
      colPos: "line:col",
      discard: "discard edit",
      proceed: "proceed, flag note",
      proceedTitle: "Write as-is; note is marked pii: flagged",
      markReviewed: "write, mark reviewed",
      markReviewedTitle: "Write as-is; note is marked pii: reviewed",
      redact: "redact and write",
      expires: (when: string) => `Hold expires ${when}; after that the write must be resubmitted.`,
      wroteRedacted: "written with redactions",
      wroteReviewed: "written, findings marked reviewed",
      wroteFlagged: "written, note flagged",
      discarded: "edit discarded, nothing written",
    },
    forgetDialog: {
      aria: "Forget note",
      dryRun: "dry run",
      title: (note: string) => `forget ${note}`,
      body: "The real forget path ran with a no-op writer. This is what it would remove, in one transaction:",
      file: "file",
      blocks: "blocks",
      vectors: "vectors",
      ftsRows: "fts rows",
      linksIn: "inbound links",
      linksOut: "outbound links",
      derivatives: "derivatives",
      audit: "Audit rows note.erase.requested and note.erase.completed are written. Notes that link here keep their [[link]]; it becomes intent.",
      confirm: "erase for real",
      erased: (name: string, r: { blocks: number; vectors: number; fts: number }) => `erased ${name}: ${r.blocks} blocks, ${r.vectors} vectors, ${r.fts} fts rows`,
    },
  },
  graph: {
    loading: "loading graph",
    findPlaceholder: "find node… (/)",
    findAria: "Find node",
    danglingAsIntent: "dangling as intent",
    hideIsolated: "hide isolated",
    labelsAria: "Labels",
    labelsAuto: "labels: auto",
    labelsAll: "labels: all",
    labelsNone: "labels: none",
    counts: (c: { shown: number; nodes: number; edges: number; dangling: number }) => `${c.shown}/${c.nodes} notes · ${c.edges} links · ${c.dangling} intent`,
    fit: "fit",
    zoom: "zoom",
    canvasAria: "Note graph; rings drawn as concentric circles, r0 innermost",
    hoverDangling: (degree: number) => `linked from ${degree}, no note yet`,
    hoverNode: (kind: string, degree: number) => `${kind} · ${degree} link${degree === 1 ? "" : "s"}`,
    selDangling: (degree: number) => `No note with this name. ${degree} note${degree === 1 ? "" : "s"} link to it; that is intent, not an error.`,
    selNode: (kind: string, degree: number) => `${kind} · ${degree} link${degree === 1 ? "" : "s"} · neighbours highlighted`,
    clearSelection: "Clear selection",
    recall: "recall",
    legendHint: "drag to pan · drag node to pin · double-click opens",
    keys: { find: "Find node by name", zoomIn: "Zoom in", zoomOut: "Zoom out", fit: "Fit to view", open: "Open selected / first match", clear: "Clear selection", labels: "Cycle labels auto / all / none", dangling: "Toggle dangling links" },
  },
  usage: {
    title: "Usage",
    subtitle: "what retrieval saved, and what the local model charged for it",
    loading: "reading the ledgers",
    keys: { refresh: "Refresh" },
    table: "table",
    charts: "charts",
    stat: {
      recallSaved: "recall saved",
      recallSavedSub: (returned: string, full: string) => `${returned} of ${full} tokens read`,
      findSaved: "find saved",
      findSavedSub: (returned: string, full: string) => `${returned} of ${full} lines read`,
      throughput: "model throughput",
      throughputValue: (tps: number) => `${tps} tok/s`,
      throughputSub: (tokens: string, seconds: number) => `${tokens} tokens over ${seconds} s of waiting`,
      cpu: "endpoint cpu",
      cpuValue: (cores: string) => `${cores} cores`,
      cpuNone: "not attributed",
      cpuNoneSub: "set inference.load_cgroup to attribute exactly",
      cpuSub: "average per call",
      cpuShare: (pct: number, total: number) => ` · ${pct}% of ${total}`,
    },
    dayByDay: (days: number) => `Day by day · ${days} days`,
    columns: ["day", "recalls", "tokens returned", "tokens in full", "finds", "calls", "prompt", "cached", "answer", "cores"],
    tableNote: "Days with nothing recorded are left out of this table and kept in the charts, where the axis has to stay continuous.",
    contextChart: "Context tokens per day",
    contextAside: (days: number) => `recall · ${days} days`,
    returned: "returned",
    neverRead: "never read",
    emptyRecall: "No recall recorded in this range.",
    contextNote: "The bar is what the notes behind the hits hold in full; the coloured part is what was handed over. The rest is the ceiling that reading them whole would have cost — a saving only where reading them whole was the real alternative.",
    promptChart: "Model prompt tokens per day",
    promptAside: (cached: string, prompt: string) => `${cached} of ${prompt} from cache`,
    fromCache: "from cache",
    processed: "processed",
    emptyCalls: "No model call recorded in this range.",
    promptNote: "Cache hits are prompt tokens the endpoint did not process again. On a local model that buys time, not money — nothing here was billed and nothing left this machine. Calls made before the cache figure was recorded report none, which makes the cached share a floor.",
    cpuChart: "CPU during model calls",
    unattributed: (n: string) => `${n} calls unattributed`,
    coresLegend: "cores burned by the endpoint",
    coresInMachine: (n: number) => `${n} cores in this machine`,
    coresAria: "cores burned per day",
    cpuNote: "Cores the endpoint's own cgroup burned, averaged per day. Exact attribution, and it exists only because inference.load_cgroup names that cgroup; without it the page falls back to machine-wide figures and says so. The dashed line is what this machine has.",
    hoverDay: (date: string, lower: number, lowerLabel: string, upper: number, upperLabel: string, unit: string) => `${date}: ${lower} ${lowerLabel} · ${upper} ${upperLabel} ${unit}`,
    hoverCores: (date: string, cores: string, machine: string | null) => `${date}: ${cores} cores${machine ? ` · ${machine} machine-wide` : ""}`,
    chartAria: (lower: string, upper: string) => `${lower} and ${upper} per day`,
    cost: {
      title: "What the local model cost",
      lastCall: (when: string) => `last call ${when}`,
      noCalls: "no calls yet",
      none: "No chat completion recorded. Without a configured model the contradiction check, ring proposals and session summaries stay off, and recall says so in its caveats.",
      calls: (n: string) => `${n} calls`,
      failed: (n: string) => ` · ${n} failed`,
      each: (s: number) => ` · ${s} s each`,
      prompt: "prompt",
      fromCache: "from cache",
      answer: "answer",
      noCacheFigure: (n: string) => `${n} calls reported no cache figure. `,
      noCounts: (n: string) => `${n} calls reported no counts at all.`,
    },
    memory: {
      title: "Held in memory",
      endpointSilent: "endpoint does not say",
      noApi: "This endpoint does not answer /api/ps, so what it holds in memory is unknown. Only Ollama answers it; the two OpenAI routes carry no such field, and guessing it from the outside cannot tell weights from page cache.",
      nothing: "Nothing loaded right now. The next call pays the load time before it answers.",
      resident: (size: string) => `${size} resident`,
      noVram: "none in VRAM (CPU only)",
      vram: (size: string) => `${size} in VRAM`,
      unloaded: (when: string) => `unloaded ${when}`,
      lastCall: (task: string, s: string) => `last call: ${task}, ${s} s`,
      lastCores: (cores: string) => ` · ${cores} cores`,
      lastMem: (resident: string, peak: string) => ` · ${resident} resident, peak ${peak}`,
    },
    footer: {
      ledgers: "Ledgers: usage.jsonl and load.jsonl in the store, one line per operation; model calls come from the audit log. Days are UTC.",
      unreadable: (n: string) => ` ${n} ledger rows could not be parsed and are left out.`,
      storeHealth: "store health is on Status",
    },
  },
  console: {
    title: "Command line",
    lead: "The same cyberbrain you would type at a prompt, against this store. Quotes and backslashes group arguments; there is no shell, so a semicolon or a pipe is just an argument.",
    placeholder: "recall 'what you learned'",
    inputAria: "Command",
    run: "Run",
    running: "running",
    clear: "Clear",
    empty: "Nothing has been run yet.",
    tryThese: "Try",
    refused: "Refused",
    exit: (code: number) => `exit ${code}`,
    truncated: "output cut at 256 KB",
    history: "Up and down walk what you have typed.",
    keys: { focus: "Focus the command line", clear: "Clear the output" },
  },
  terminals: {
    title: "Terminal",
    lead: "A real terminal, in this project's directory. Whatever you would run at a prompt: a shell, ssh, an agent. Quotes group arguments; this is not a shell, so a pipe or a semicolon is an argument — ask for a shell and type it there.",
    placeholder: "ssh -o ServerAliveInterval=60 root@example.com",
    commandAria: "Command to run in a terminal",
    open: "Open",
    openShell: "Shell",
    shell: "shell",
    close: "close",
    empty: "No terminal is open.",
    emptyHow: "Type a command, or open a plain shell.",
    noToken: "This address carries no terminal token.",
    noTokenHow: "A terminal is offered only by `cyberbrain serve --terminal`, and only at the address it prints — the token is in that address. Open that one, or use the desktop launcher.",
    saved: "saved:",
    save: "Save",
    namePrompt: "A name for this connection:",
    forget: (name: string) => `forget ${name}`,
    savedIn: (path: string) => `kept in ${path}, per user and per machine — not in the store, which travels with the repository`,
    paneLabel: (what: string) => `terminal running ${what}`,
    exited: (code: number) => `exited with code ${code}`,
    state: { opening: "opening", open: "open", closed: "closed" },
  },
  compliance: {
    sections: { overview: "Overview", obligations: "Obligations", egress: "Egress register", audit: "Audit log", pii: "PII", retention: "Retention", models: "Model card", subject: "Subject access" },
    jumpTo: (label: string) => `Jump to ${label}`,
    profile: "profile",
    profileEu: "GDPR, EU AI Act",
    profileCh: "revised FADP",
    profileOff: "checks compiled in, disabled",
    loadingEgress: "reading egress register",
    overview: {
      nothingLeft: "Nothing has left this machine.",
      nothingLeftSub: (days: number, enabled: number) => `In ${days === 1 ? "one day" : `${days} days`} no registered path sent a byte to a public destination. ${enabled ? `${enabled} such path${enabled === 1 ? " is" : "s are"} enabled and unused.` : "No such path is currently enabled."}`,
      noContent: "No note content has left this machine.",
      noContentSub: (days: number, uses: number, size: string, when: string, disabled: boolean) =>
        `The only outbound traffic to the internet in ${days === 1 ? "one day" : `${days} days`} was ${uses} model download${uses === 1 ? "" : "s"} (${size} sent, on your consent, ${when}). That request carried no note text, no identifier, no telemetry. ${disabled ? "The download path is now disabled: the artefact is present and hash-verified." : ""}`,
      bytesLeft: "Bytes have left this machine.",
      bytesLeftSub: (uses: number, days: number, size: string) => `${uses} outbound request${uses === 1 ? "" : "s"} to public destinations in ${days === 1 ? "one day" : `${days} days`} (${size}). See the register below for which purposes.`,
      since: (date: string) => `since ${date}`,
      internet: "to the internet",
      noRequests: "0 requests",
      requests: (n: string) => `${n} request${n === "1" ? "" : "s"}`,
      noBytes: "no bytes to any public host",
      bytesOut: (size: string, purposes: string) => `${size} out · ${purposes}`,
      ownNetwork: "to your own network",
      calls: (n: string) => `${n} call${n === "1" ? "" : "s"}`,
      noLocalEndpoint: "no local endpoint configured",
      refused: "refused by the wrapper",
      refusedSub: "attempts outside policy, each logged",
      refusedNone: "no attempt outside policy",
      auditRows: "audit rows",
      auditRowsSub: "append-only, exportable",
      howKnown: "how this statement is known, not hoped",
      how1: (hash: string) => `The list of purposes is closed at compile time (register hash ${hash}). A path not on it cannot be built.`,
      how2: "All outbound I/O goes through one wrapper that requires a registered purpose; CI fails on an HTTP client constructed anywhere else.",
      how3: "Every use of a path writes an egress.permitted row and, when the request closes, an egress.completed row with the bytes. “Uses” count the former, “bytes out” sum the latter; a request that never closed counts as a use with no bytes.",
      how4: "The inference endpoint must be loopback or private-range unless allow_public_endpoint is set; a refusal is logged as policy.refusal.",
      how5: "The core embeds statically; no model server, no system library, no runtime fetch. The UI you are reading is served from the binary and fetches nothing external.",
      how6: "Telemetry is not a purpose. There is no opt-out because there is nothing to opt out of.",
    },
    obligations: {
      title: "What this profile claims",
      aside: (law: string) => `profile encodes ${law}`,
      intro:
        "The catalogue behind the profile, printed rather than implied. Every line names the article it rests on and how sure the author is of it, because a compliance claim without its uncertainty is a claim about the author's confidence, not about the law.",
      colTopic: "topic",
      colSummary: "what it says",
      colBasis: "basis",
      colConfidence: "confidence",
      confidence: { low: "low", medium: "medium", high: "high" },
      footer:
        "The confidence is an engineer's, not counsel's. High means the rule was verified in the primary text; medium means the substance is right but a number or an exception is from memory; low means do not repeat it to a regulator without checking. Nothing below high drives behaviour in the binary.",
      topics: {
        scope: "Scope",
        "legal-basis": "Legal basis",
        erasure: "Erasure",
        access: "Access",
        "breach-notification": "Breach notification",
        "processing-register": "Processing register",
        "data-protection-officer": "Data protection officer",
        "impact-assessment": "Impact assessment",
        "cross-border-transfer": "Cross-border transfer",
        sanctions: "Sanctions",
        "ai-regulation": "AI regulation",
      },
    },
    egress: {
      register: (hash: string) => `register ${hash}`,
      note: "Every code path that can send bytes off the machine is registered at compile time in one module; all outbound I/O goes through a single wrapper that takes one of these purposes, and CI fails on any HTTP client built elsewhere. A purpose not on this list does not exist in the binary. Telemetry is not on the list.",
      colPurpose: "purpose",
      colState: "state",
      colDestination: "destination",
      colData: "what is sent",
      colPermitted: "permitted by",
      colUses: "uses",
      colBytes: "bytes out",
      colLast: "last used",
      enabled: "enabled",
      disabled: "disabled",
      carries: "carries note content",
      carriesNot: "no note content",
    },
    pii: {
      title: "PII findings",
      scanOn: "write-time scan on",
      scanOff: "scan off (profile off)",
      held: (n: number) => `${n} write${n === 1 ? "" : "s"} held`,
      awaiting: "awaiting a decision:",
      colNote: "note",
      colState: "state",
      colFindings: "findings (masked)",
      colReviewed: "reviewed",
      neverScanned: "never scanned",
      unscannedTitle: "No write-time scan ever ran over this note (imported or hand-written). The findings column is a scan of the body as it is now.",
      unscannedNothing: "a scan of the body now finds nothing; the note stays unscanned until it is written through the tool",
      nothingNow: "nothing found in the body now",
      emptyTitle: "Every note was scanned and none carries reviewed or flagged personal data.",
      emptyBody: "Every write under profile eu/ch is scanned for e-mail addresses, IPs, API keys, IBANs and phone numbers before it lands. Heuristic: a seatbelt, not a guarantee. A note nobody scanned would be listed here as “not scanned”.",
    },
    audit: {
      title: "Audit log",
      rowsMatch: (n: string) => `${n} rows match`,
      export: "export JSON",
      exported: (n: number) => `exported ${n} rows`,
      families: {
        note: "note.* (every write, hold, erase)",
        "note.write": "note.write.* (writes, holds, resolutions)",
        "note.erase": "note.erase.* (forget, retention)",
        egress: "egress.* (permitted, completed, failed, abandoned)",
        "policy.refusal": "policy.refusal",
        "inference.call": "inference.call",
        "retention.expired": "retention.expired",
        "subject.access": "subject.access",
        index: "index.* (cleared, note dropped, profile changed)",
        "store.init": "store.init",
        "audit.export": "audit.export",
        "consent.model-download": "consent.model-download",
      },
      anyAction: "any action",
      actionAria: "Action family",
      actionTitle: "Filters by family prefix: note selects note.write, note.erase and their sub-actions",
      actorPlaceholder: "actor prefix",
      actorAria: "Actor",
      searchPlaceholder: "subject or detail contains…",
      searchAria: "Search audit",
      colSeq: "seq",
      colTime: "time",
      colActor: "actor",
      colAction: "action",
      colSubject: "subject",
      colDetail: "detail",
      noDetail: "no detail on this row",
      nestedTitle: "nested value, shown as its JSON text",
      empty: "No audit rows match.",
      older: "older rows",
    },
    retention: {
      title: "Retention queue",
      summary: (withRetention: number, due: number) => `${withRetention} with a retention · ${due} due`,
      invalid: (n: number) => `${n} invalid`,
      indefinite: (n: number) => `${n} kept indefinitely`,
      dryRun: "dry-run apply",
      dryRunTitle: "Runs the real erase path with a no-op writer and shows what would go",
      colNote: "note",
      colRetention: "retention",
      colExpires: "expires",
      colState: "state",
      invalidTitle: "No expiry can be computed from an invalid duration.",
      invalidPill: (reason: string) => `invalid: ${reason}`,
      due: "due — awaiting apply",
      kept: "kept",
      emptyTitle: (all: string | null) => `No note carries a retention duration; ${all ? `all ${all} notes are` : "everything is"} kept indefinitely.`,
      more: (n: number) => `${n} more, sorted by expiry`,
      note: "Expiry never happens in the background. A due note stays until you apply, and applying goes through the same path as forget: file, blocks, vectors, FTS rows and links in one transaction, one audit row each.",
      applied: "applied",
      wouldErase: (n: number) => `${n} note${n === 1 ? "" : "s"} would be erased`,
      erased: (n: number) => `${n} note${n === 1 ? "" : "s"} erased`,
      erasedToast: (n: number) => `erased ${n} note${n === 1 ? "" : "s"} through the forget path`,
      applyReal: "apply for real",
      removedLine: (r: { blocks: number; vectors: number; fts: number; links: number }) => `${r.blocks} blocks, ${r.vectors} vectors, ${r.fts} fts, ${r.links} links`,
      skipped: "skipped, with the reason",
    },
    models: {
      title: "Model card",
      aside: "EU AI Act transparency · what generates or embeds on this machine",
      inUse: "in use",
      source: "source",
      licence: "licence",
      licenceNone: "not stated by the source; never guessed",
      hash: "hash",
      hashNone: "not held (endpoint model; weights are not on this machine)",
      format: "format",
      dimension: "dimension",
      poolingNone: "pooling not stated",
      dimNotApplicable: "not applicable (generates text)",
      size: "size",
      notHeld: "not held",
      verified: "verified",
      verifiedOnLoad: (when: string) => `${when} on load`,
      verifiedNoStamp: "hash checked on every load; no timestamp of that check is kept",
      nothingToVerify: "nothing to verify",
      intendedUse: "intended use",
      limitations: "limitations",
    },
    subject: {
      title: "Subject access request",
      aside: "GDPR Art. 15 · FADP Art. 25",
      placeholder: "name, e-mail or handle",
      aria: "Identifier",
      submit: "search everything",
      note: "Searches every note, block and audit row for the identifier and lists what is held, with citations, in a form that can be handed to the person. The search itself writes an audit row.",
      hits: (n: number, identifier: string, notes: string, blocks: string, rows: string) => `${n} hit${n === 1 ? "" : "s"} for ${identifier} across ${notes} notes, ${blocks} blocks, ${rows} audit rows`,
      deadline: "response due:",
      nothing: "Nothing found for this identifier in what was searched; read the caveats above for what a substring search cannot see.",
    },
  },
  /**
   * Simple mode. The words here carry more of the design than the CSS does: this is the
   * half of the page written for somebody who never types a command, so nothing in it names
   * a command, a ring number or a citation.
   */
  mode: {
    simpleName: "Simple",
    expertName: "Full",
    toExpert: "Full view",
    toSimple: "Simple view",
    title: "How much of the tool to show",
  },
  ask: {
    navLabel: "Ask",
    placeholder: "What are you looking for?",
    aria: "Your question",
    hint: "Ask in your own words. Whole sentences are fine.",
    examples: ["Can we release on a Friday?", "Who signs off a release?", "What applies to credentials?"],
    thinking: "looking…",
    appliesAlways: "Applies always, no exceptions.",
    applies: ["Applies always, no exceptions.", "This is how the work is done here.", "Written down as knowledge.", "Noted during a session.", "Came from outside and has not been checked."],
    changed: (when: string) => `Last changed ${when}`,
    openNote: "Open the note",
    sources: "Where this comes from",
    conflict: "Something written later says otherwise. The rule above is the one that counts.",
    conflictWhy: "A rule set by the operator outranks anything noted afterwards.",
    nothing: (q: string) => `Nothing written down about “${q}”.`,
    nothingHint: "Try fewer words, or ask a colleague — and if the answer turns out to matter, it belongs in a note.",
    allGood: "Everything is in order.",
    checking: "checking…",
    trouble: "Something needs attention.",
    details: "Technical details",
  },
  simpleNotes: {
    title: "Notes",
    count: (n: number) => `${n} note${n === 1 ? "" : "s"}`,
    filterAria: "Filter by title",
    filterPlaceholder: "Filter…",
    noMatch: "No note has that in its title.",
    emptyTitle: "Nothing written down yet.",
    emptyBody: "Cyberbrain keeps what gets decided while you work: a rule, a reason, something that has already gone wrong once.",
    emptyHow: "How does something get here?",
    emptyHowBody: "Mostly the assistant writes it while you work with it. You can also write one yourself on the command line:",
  },
  team: {
    title: "In the team",
    subtitle: "what was written down most recently",
    empty: "Nobody has written anything down yet.",
    by: (who: string) => `by ${who}`,
  },
};
